POT regeneration evidence — release v0.4.8

statnive.php             mtime: 2026-05-04 06:48:05  (version-bump touched)
languages/statnive.pot   mtime: 2026-05-03 14:08:16  (last regen for v0.4.7)

Status: STALE — POT mtime is OLDER than statnive.php.

Per /statnive-release Step 7, POT must be newer than statnive.php before SVN push.
Local regen needs wp-env (currently blocked by 3 env issues — see gate-run.log).
Two paths forward:
  1. Fix wp-env env blockers, run:
       npx wp-env run cli wp i18n make-pot /var/www/html/wp-content/plugins/statnive languages/statnive.pot
     and amend this commit with the regen.
  2. Regenerate via CI on the release tag and let the SVN-push step pick up the
     fresh POT from the CI artifact.

Either way: do not push to SVN until languages/statnive.pot mtime > statnive.php mtime.

v0.4.8 introduced no new translatable strings vs v0.4.7 (no UI copy changes;
the FrontendHandler change has no user-facing string, only internal logic, and
the readme/changelog/version bumps are not translated). A regen will be a
date/header-only change.
